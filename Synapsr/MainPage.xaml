<Page
    x:Class="Synapsr.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Synapsr"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ItemsControl x:Name="MessagesContainer">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Background="{ThemeResource SystemAccentColor}" Orientation="Horizontal" Margin="5">
                        <Image Source="{Binding AvatarUrl}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"></Image>

                        <StackPanel Orientation="Vertical" Margin="5,0,0,0">
                                <TextBlock Text="{Binding Username}" Grid.Row="0"></TextBlock>
                                <TextBlock Text="{Binding Message}" Grid.Row="1"></TextBlock>
                        </StackPanel>

                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
